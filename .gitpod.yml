
image: dclong/jupyterhub-ds
tasks:
  - command: export PATH=/home/gitpod/.local/bin:$PATH
  - command: ln -s /workspace/blog/main.py /workspace/.pip-modules/bin/blog
  - command: nvim --headless +"call dein#install()" +qall
  - command: xinstall svim -ic
ports:
  - port: 8888
